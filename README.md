# poc_edu_archive_webscraper
a proof of concept edu archive webscraper 

Python version: 3.7 

Run the following commands:
 * `pip install requests` 
 * `pip install beautifulsoup4`
 * `python basic_scraper.py`
